# ==========================================
# 核心依赖 (我们刚才写的所有代码真正运行需要的库)
# ==========================================

#用这个命令直接安装 pip install -r requirements.txt 

# 1. 数据处理核心 (GUI、Web、报告生成都需要)
pandas>=2.0.0

# 2. 网络请求 (用于抓取外部 API 或网页)
requests>=2.31.0

# 3. 本地模拟服务器 (用于搭建我们自己的测试后端)
Flask>=3.0.0

# 4. Web UI 数据大屏 (用于替代本地 GUI 的现代 Web 界面)
streamlit>=1.30.0

# 5. 打包工具 (用于将 Python 脚本打包为独立的 .exe)
pyinstaller>=6.3.0


# ==========================================
# 进阶拓展包 (如果你想实现之前讨论的额外功能，可以取消注释)
# ==========================================

# --- 静态网页爬虫解析 ---
# beautifulsoup4>=4.12.0

# --- 操作 Excel 报告 ---
# openpyxl>=3.1.2
# xlsxwriter>=3.1.9

# --- 纯 Python 后台生成 PDF (不需要前端 JS 的备选方案) ---
# Jinja2>=3.1.3
# pdfkit>=1.0.0

# --- 静态数据可视化画图 ---
# matplotlib>=3.8.0
# seaborn>=0.13.0